# Skills

## Flutter UI/UX Development
Building responsive interfaces with Material Design 3, implementing forms, and navigation.

## Firebase Integration
Configuring and implementing Firebase Auth for login/registration, and Firestore for database CRUD operations.

## Real-time Data Handling
Utilizing `StreamBuilder` and Firestore Snapshots to monitor real-time data changes in queue status.

## FCFS Logic Implementation
Strictly managing the queue using the `ORDER BY created_at ASC` query with millisecond precision via `FieldValue.serverTimestamp()`.